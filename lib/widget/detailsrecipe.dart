import 'package:flutter/material.dart';

class CustomRecipe extends StatelessWidget {
  String subtitle;
  CustomRecipe(this.subtitle);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
